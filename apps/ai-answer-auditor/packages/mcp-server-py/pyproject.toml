[project]
name = "ai-answer-auditor-mcp"
version = "0.1.0"
description = "MCP server wrapper exposing AI Answer Auditor as an agent tool."
requires-python = ">=3.10"
dependencies = [
    "ai-answer-auditor",
    "mcp>=1.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["server"]
