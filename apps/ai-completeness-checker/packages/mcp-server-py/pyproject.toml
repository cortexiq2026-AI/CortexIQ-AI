[project]
name = "ai-completeness-checker-mcp"
version = "0.1.0"
description = "MCP server wrapper exposing AI Completeness Checker as an agent tool."
requires-python = ">=3.10"
dependencies = [
    "ai-completeness-checker",
    "mcp>=1.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["server"]
